# Souveno expo: 3-day subsidy, early-bird and confirmation follow-up

Schedule: 09:30 every 3rd day IST (cron `0 4 */3 * *` UTC) · id `trig_01RX1jC3VGsdzmdQbcCymLMC`

Connectors needed: Gmail (souveno30@gmail.com), Google Calendar; repository santoshdotai/hellomass with push access.

## Prompt

```
SETUP FIRST (this session starts with no repository): call the add_repo tool with owner "santoshdotai", repo "hellomass", access "push"; run the clone command it returns so the repo lands at /home/user/hellomass; call register_repo_root with that path; then `cd /home/user/hellomass && git fetch origin claude/nice-pasteur-omkzao && git checkout claude/nice-pasteur-omkzao`. Run `pip install -r expo-backend/requirements.txt` if imports fail. If the Gmail connector tools (mcp__Gmail__*) are not available, skip the mail steps, write the summary to expo-backend/docs/briefs/followup-<date>.md and start the commit message with "[no Gmail connector]".

You are the Souveno AI Expo Agent's follow-up run (every 3 days, 9:30am IST). Repo: santoshdotai/hellomass, branch claude/nice-pasteur-omkzao, folder expo-backend (run python and pytest from inside it). Catalogue: data/expo/events.json (36 shows in the plan + 3 marked attend:"skip"; `_meta.contact_policy` = company e-mail souveno30@gmail.com for stalls/organisers/subsidies, travel e-mail santoshdotai@gmail.com for flights/hotels; all payments from the Souveno current account). Company: Souveno AI Solutions, GSTIN 36BDNPP2011D2ZV, Udyam micro UDYAM-TS-02-0251582, proprietorship, Hyderabad, 1-year-old startup, founder Santosh Padmaa aged 41. Subsidy engine: backend/core/expo/subsidy.py and each event's `subsidy` block. Funds catalogue: data/expo/funds.json (46 programmes, fields deadline/rolling/status/last_checked/notes) and data/expo/pavilions.json (17 Indian pavilions abroad). After changing data, rebuild the phone dashboard: `python scripts/build_dashboard.py ../expo-frontend/dashboard/command_center_template.html ../expo-frontend/dashboard/souveno-expo-command-center.html` and republish it to https://claude.ai/code/artifact/6948e7c2-1876-4085-9518-5249e110da3f with the Artifact tool (pass the url). Dashboard shared db collections: approvals, plans, actuals, settings, funds, targets, applications (Artifact tool read_db / write_db with that url).

Do all of this, then stop:
1. PMS APPROVED-FAIR LIST (most important): the MSME PMS reimbursement applies ONLY to fairs on the DC-MSME approved list shown under "Trade Fairs Domestic" on my.msme.gov.in (https://my.msme.gov.in/MyMsme/Reg/COM_Fair.aspx, also mirrored in DC-MSME circulars on dcmsme.gov.in). For every domestic exhibit show in events.json, check whether the fair (this edition, these dates) is on that list. Set the event's `subsidy.pms_listed` to "yes" or "no" (never guess; leave "unknown" if the page cannot be read) and `subsidy.pms_list_checked` to today's date, with the source URL in `subsidy.sources`. Plastivision India 2027 was checked by Santosh on 11 Sep 2026 and was NOT listed: keep watching it, and if it is not listed, also check plastivision.org/msme-subsidy.php and AIPMA for an MSME pavilion or a pending DC-MSME approval and record what you find in `subsidy.notes`. Any change from "no"/"unknown" to "yes" goes at the top of the e-mail under "APPLY NOW" with the apply-by date (30 days before the show).
2. SUBSIDIES: for every event whose `subsidy.status` is not "confirmed", or whose last_checked is older than 3 days, check the web for the organiser's early-bird / space-booking deadline and rate, PMS / pavilion eligibility, the current MSME IC scheme call (https://www.ic.msme.gov.in/), and the Telangana MSME Policy 2024 marketing-assistance % and cap. Write findings into the event's `subsidy` block (early_bird_deadline YYYY-MM-DD or null, early_bird_note, scheme_overrides, sources, last_checked, status "confirmed" only from the organiser or ministry page). Never invent a date. Also verify the PMS ceiling actually shown on my.msme.gov.in / the 2021 PMS guidelines (₹30,000 + ₹15,000 contingency vs up to ₹1.5 lakh per event) and put the confirmed figure into `SCHEMES["pms"]["benefit"]` in subsidy.py with the source. Confirm the three tentative AI-circuit dates (Telangana Rising Global Summit Dec 2026, SaaSBoomi Annual 2027, Nasscom NTLF 2027) and set tentative:false with the source once published.
3. FUNDS & PAVILIONS: for every entry in funds.json and pavilions.json with status "to_verify" or last_checked older than 3 days, check the programme page for the current call / cohort deadline, amount and eligibility (SISFS windows via T-Hub / CIE IIIT-H / IITH, T-AIM Revv Up cohort, MeitY SAMRIDH / TIDE calls, Hub71 cohort dates, LEAP 2027 India pavilion via ESC/NASSCOM, GITEX India pavilion, EEPC/IEEMA pavilion circulars). Update deadline, amount, notes, last_checked and status. If a deadline is within 30 days and the dashboard db `funds/<id>` shows status not_applied, put it at the top of the e-mail under "APPLY NOW". Remember Souveno is a proprietorship: DPIIT-only programmes stay "not_eligible_yet".
4. TARGETS & COMPETITORS: for every exhibit show starting within 45 days, fetch the organiser's published exhibitor list; score exhibitors against the ICP segments and write the top 30 with stand/hall and a one-line reason into the dashboard db doc `targets/<eventId>` as {eventId, rows:[{company, stand, hall, segment, why, intro}], builtAt}. List WhatsApp-automation / CRM / video-analytics vendors exhibiting into the event's `competitors` array in events.json as [{name, stall}].
5. ORGANISER REPLIES: if Gmail is available (souveno30@gmail.com inbox), search replies from each event's exhibitor_contact (last 3 days); note stall numbers, rate cards, invoices, early-bird offers into `subsidy.notes` / `stall.rate_status`; if a proforma amount appears, update the dashboard approval `approvals/<eventId>--stall_advance` (amountInr, details.rate_status = "organiser invoice") and set `plans/<eventId>.organiserStage` with organiserAt = now.
6. CONFIRMATIONS: flights and hotels are booked with santoshdotai@gmail.com; mails are forwarded to souveno30@gmail.com. Search the last 3 days for Booking.com confirmations, airline / MakeMyTrip e-tickets (PNR) and organiser payment receipts (UTR); mark the matching approval executed (write_db update approvals/<id> {status:"executed", note:"<PNR/confirmation/UTR>", executedAt, source:"gmail"}) and set plans/<eventId> flightStatus/hotelStatus/stallStatus = "booked". Never store card numbers, passwords or PINs.
7. ACTUALS: for any dashboard db `actuals/<eventId>` not yet reflected in events.json, append the observed footfall to footfall_history (note "Souveno's own count") and set expected.visitors.
8. Run `python3 -m pytest tests -q` in expo-backend, rebuild and republish the dashboard, commit ("Expo agent 3-day follow-up <date>") and push to claude/nice-pasteur-omkzao. Also push the same expo-backend and expo-frontend folders to the `expo-backend` and `expo-frontend` branches with `git subtree push --prefix=expo-backend origin expo-backend` and `git subtree push --prefix=expo-frontend origin expo-frontend`.
9. E-mail santoshdotai@gmail.com one summary (APPLY NOW items first, then the PMS approved-list status per show, subsidies, early-bird deadlines, fund/pavilion changes, targets built, organiser replies, confirmations matched, actions needed). If Gmail is unavailable, write it to expo-backend/docs/briefs/followup-<date>.md and push.
Do not book, pay or apply for anything. Do not ask questions; there is no human in this session.
```
