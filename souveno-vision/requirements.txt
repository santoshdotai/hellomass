# Souveno Vision Intelligence — Python 3.10 / 3.11 (tested on 3.11)
# Install:  pip install -r requirements.txt
# CPU-only PyTorch (smaller, recommended on laptops without NVIDIA GPU):
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# See LICENSING.md for the licence of every major dependency.

# Web / API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
websockets==14.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Config / utilities
pyyaml==6.0.2
python-dotenv==1.0.1
loguru==0.7.3
requests==2.32.3
psutil==6.1.0
numpy==1.26.4
pandas==2.2.3

# Storage (legacy café demo uses SQLAlchemy; the new layer uses the stdlib sqlite3)
sqlalchemy==2.0.36

# Computer vision / AI
opencv-python-headless==4.10.0.84
ultralytics==8.3.55          # AGPL-3.0 — commercial licence required for closed deployment (LICENSING.md)
torch==2.5.1
torchvision==0.20.1
supervision==0.25.1          # MIT — ByteTrack implementation

# Optional: commercially clean inference path once a suitably licensed ONNX model is chosen
# onnxruntime==1.20.1

# Testing
pytest==8.3.4
httpx==0.28.1
